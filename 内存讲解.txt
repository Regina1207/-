GC：浏览器的垃圾回收机制（内存的一种机制，不占内存，准确来说属于浏览器）
最主要的两种清除法：
标志清除：贴标签
    分为两个阶段,标记和清除
    标记：进入环境（执行环境），GC进行标记（变量使用过程中），使用完了，离开环境，被再次标记
    清除：把离开环境时被标记变量,通过GC主动地回收（清除）
        缺点：容易造成内存泄漏（堆栈溢出）
        递归，复杂的循环难以标记，不会进行回收，造成大量的变量存在于内存中
引用计数清除：计数  常用的
    1.命名变量时，默认的起始数据为1
    2.当被赋值引用时，数量加一 
    3.释放引用时，数量减一
    4.当变量的数为0时，则被认为是垃圾，GC会主动回收它
    var a = {} ;   1
    var b = a ;    2
    var c = b ;    3
    var c =null ;  2
    var b =null ;  1
    var a =null ;  0
JS事件循环：

    1.浏览器安全性
    2.优化：长列表（雅虎军规），微前端
    3.垃圾回收

createDocumentFragment():是一次性创建的一个DOM片段

requestAnimationFrame():
    是一个帧动画：是H5中新增的动画
    优点：
        1.不会丢失关键帧
        2.是直接在CPU上工作，效率高
        3.刷新频率  60HZ  根据页面的刷新而刷新
    场景：长列表滚动过度

函数节流:
    函数节流，就是控制请求的次数，主要用于频繁的获取数据（get请求）
    场景：搜索功能
    function getInfo(val){
        if(val>=4){
            ajax(){
                url:'jjj',
                method:'git',
                success:(data)=>{

                }
            }
        }
    }
    getInfo(document.getElementById('ipt').value.length);
函数防抖:
    定时器越来越快，以指数的方式递增
